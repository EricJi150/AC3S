[build-system]
requires = ["setuptools>=61"]
build-backend = "setuptools.build_meta"

[project]
name = "ac3s"
version = "1.0.0"
description = "Official implementation of AC3S"
readme = "README.md"

[tool.setuptools.packages.find]
where = ["src"]